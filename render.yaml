services:
  - type: web
    name: meno-qr-bill-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    envVars:
      - key: QRBILL_API_KEY
        sync: false
